;-----------------------------------
;기본치 계산에 앞서 처리하는 것
;-----------------------------------
@SKILL_149_SP_0_EXIST
RETURN 1

@SKILL_149_SP_0_NAME
RESULTS = 앱설루트 루더-

;레벨은 1-5까지
@SKILL_149_SP_0_LEVEL
RETURN 3

;대상 선택
@SKILL_149_SP_0_SETTARGET(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
SIF BATTLE_COMMANDER_NUM:대상측 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:대상측)
RETURN 1

;발동 판정
@SKILL_149_SP_0_CAN_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측
RETURN BATTLE_COMMANDER_NUM:발동측 > 1 && BATTLE_COMMANDER_NUM:대상측 >= 1 

;발동 텍스트를 오바라이드 하고 싶을 때에.
;「아무개의 스킬 발동!   00했다!」의 「00했다!」의 부분을 실장하고 싶은 경우는,
;이것이 아니고 INVOKE로 쓰면 좋습니다.
;@SKILL_149_SP_0_INVOKE_TEXT(발동자, 스킬, 장르)
;#DIM 발동자
;#DIM 스킬
;#DIMS 장르

;효과를 여기에 기술
@SKILL_149_SP_0_INVOKE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
대상측 = !발동측

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:발동측
	SIF LOCAL == 발동번호
		CONTINUE
	LOCAL:1 = LOCAL
NEXT

PRINTFORML %ANAME(발동자)%의 힘으로 %ANAME(BATTLE_COMMANDER:발동측:(LOCAL:1))%와 %ANAME(BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)%가 바뀌었다!

LOCAL:2 = BATTLE_COMMANDER:발동측:(LOCAL:1)
LOCAL:3 = BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET

BATTLE_COMMANDER:발동측:(LOCAL:1) = LOCAL:3
BATTLE_무투:발동측:(LOCAL:1) = ABL:(LOCAL:3):무투
BATTLE_방위:발동측:(LOCAL:1) = ABL:(LOCAL:3):방위
BATTLE_지략:발동측:(LOCAL:1) = ABL:(LOCAL:3):지략
BATTLE_정치:발동측:(LOCAL:1) = ABL:(LOCAL:3):정치
BATTLE_가창:발동측:(LOCAL:1) = ABL:(LOCAL:3):가창
BATTLE_요리:발동측:(LOCAL:1) = ABL:(LOCAL:3):요리
BATTLE_요술:발동측:(LOCAL:1) = ABL:(LOCAL:3):요술
BATTLE_무투파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_무투:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_방위파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_방위:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_지략파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_지략:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_요술파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_요술:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_가창파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_가창:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_요리파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_요리:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_요술파워:발동측:(LOCAL:1) = ABL_POWER(BATTLE_요술:발동측:(LOCAL:1), BATTLE_COMMANDER:발동측:(LOCAL:1))
BATTLE_SUMMONED_CHARA:발동측:(LOCAL:1) = 1

BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET = LOCAL:2
BATTLE_무투:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):무투
BATTLE_방위:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):방위
BATTLE_지략:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):지략
BATTLE_정치:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):정치
BATTLE_가창:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):가창
BATTLE_요리:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):요리
BATTLE_요술:대상측:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):요술
BATTLE_무투파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_무투:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_방위파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_방위:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_지략파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_지략:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_요술파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_요술:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_가창파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_가창:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_요리파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_요리:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_요술파워:대상측:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_요술:대상측:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:대상측:COMBAT_SKILL_TARGET)
BATTLE_SUMMONED_CHARA:대상측:COMBAT_SKILL_TARGET = 1
RETURN 1

@SKILL_149_SP_0_EXPLANATION
RESULTS = 아군 한사람과 적한사람을 바꿔 넣는다.

@SKILL_149_SP_0_CANT_TELL

@SKILL_149_SP_0_RATE(발동자, 발동번호, 발동측, 발동세력, 발동부대, 대상세력, 대상부대)
#DIM 발동자
#DIM 발동번호
#DIM 스킬
#DIMS 장르
#DIM 발동측
#DIM 발동세력
#DIM 발동부대
#DIM 대상측
#DIM 대상세력
#DIM 대상부대
RETURN 250

