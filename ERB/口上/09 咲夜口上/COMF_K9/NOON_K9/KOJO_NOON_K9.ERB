;─────────────────────────────────────── 
;●커맨드 실행 전날상 300～
;─────────────────────────────────────── 
@KOJO_K9_NOON(사쿠야_대상,전후)
#DIM 사쿠야
#DIM 사쿠야_대상
#DIMS 전후
#DIMS 사쿠야구상실대카테고리

IF !사쿠야_대상
	사쿠야_대상 = MASTER
ENDIF

;리셋트
사쿠야 = NAME_TO_CHARA("사쿠야")
사쿠야구상실대카테고리 '= ""

;─────────────────────────────────────── 
;●상세 카테고리 결정
;─────────────────────────────────────── 
IF IS_MPLY(사쿠야)
	사쿠야구상실대카테고리 '= "실행"
ELSE
	사쿠야구상실대카테고리 '= "사쿠야_대상"
ENDIF

;─────────────────────────────────────── 
;●상세 카테고리에 의한 분기
;─────────────────────────────────────── 
IF 사쿠야구상실대카테고리 == "실행"
	;전문이나 후문인가
	IF 전후 == "전"
		CALL KOJO_K9_NOON_BEFORE_PLAYER(사쿠야_대상)
	ELSEIF 전후 == "후"
		CALL KOJO_K9_NOON_AFTER_PLAYER(사쿠야_대상)
	ENDIF
ELSEIF 사쿠야구상실대카테고리 == "사쿠야_대상"
	;전문이나 후문인가
	IF 전후 == "전"
		CALL KOJO_K9_NOON_BEFORE_TARGET(사쿠야_대상)
	ELSEIF 전후 == "후"
		CALL KOJO_K9_NOON_AFTER_TARGET(사쿠야_대상)
	ENDIF
ENDIF

RESETCOLOR
RETURN RESULT

