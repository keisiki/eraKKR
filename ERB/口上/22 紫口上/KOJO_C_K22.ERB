;=================================================
;●「침실호출」의 개시시의 대사 ※단독으로 불렸을 경우만 불린다
;=================================================
@KOJO_TRAIN_START_C_K22
;[공허]상태라면 구상을 표시하지 않는다
IF TALENT:공허
	RETURN
ENDIF
IF KDVAR:유카리_맛봐얻지않는즐거움_성전환중
	PRINTFORMW 「자, 나에게 몸을 맡겨……」
	PRINTFORMW 「절대로 맛볼 수 없는 쾌락에, 빠져 버리세요……♥」
	TALENT:MASTER:성별 = !TALENT:MASTER:성별
ENDIF



;=================================================
;●「침실호출」의 종료시의 대사 ※단독으로 불렸을 경우만 불린다
;=================================================
@KOJO_TRAIN_END_C_K22
#DIM 보수
;[공허]상태라면 구상을 표시하지 않는다
IF TALENT:공허
	RETURN
ENDIF

IF KDVAR:유카리_맛봐얻지않는즐거움_성전환중
	PRINTFORMW 「후후, 수고 하셨습니다……」
	PRINTFORMW 「자, 원래에 되돌려 올린다. 가만히 해 주세요」
	PRINTFORMW %ANAME(MASTER)%의 신체가, 원래에 돌아와 간다……
	PRINTFORMW 「또, 언제라도 해 주어요. %조사처리(ANAME(MASTER),"가")% 바란다면요」
	PRINTFORMW %조사처리(ANAME(TARGET),"는")% 몸치장을 준비하면, 방을 뒤로 했다……
	KDVAR:유카리_맛봐얻지않는즐거움_성전환중 = 0
	TALENT:MASTER:성별 = !TALENT:MASTER:성별
ENDIF

;=================================================
;●커맨드 실행 후(파라미터의 변동 처리가 끝나고 나서 불려 간다)
;=================================================
@KOJO_COM_AFTER_C_K8
;[공허]상태의 경우는 구상을 표시하지 않는다
IF TALENT:공허
	RETURN 0
ENDIF
IF KDVAR:유카리_맛봐얻지않는즐거움_성전환중
	BASE:체력 = MAXBASE:체력
	BASE:기력 = MAXBASE:기력
ENDIF
