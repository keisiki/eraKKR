;─────────────────────────────────────── 
;●커맨드 실행 전날상 300～
;─────────────────────────────────────── 
@KOJO_K10_NOON(레밀리아_대상,전후)
#DIM 레밀리아
#DIM 레밀리아_대상
#DIMS 전후
#DIMS 레밀리아구상실대카테고리

IF !레밀리아_대상
	레밀리아_대상 = MASTER
ENDIF

;리셋트
레밀리아 = NAME_TO_CHARA("레밀리아")
레밀리아구상실대카테고리 '= ""

;─────────────────────────────────────── 
;●상세 카테고리 결정
;─────────────────────────────────────── 
IF IS_MPLY(레밀리아)
	레밀리아구상실대카테고리 '= "실행"
ELSE
	레밀리아구상실대카테고리 '= "레밀리아_대상"
ENDIF

;─────────────────────────────────────── 
;●상세 카테고리에 의한 분기
;─────────────────────────────────────── 
IF 레밀리아구상실대카테고리 == "실행"
	;전문이나 후문인가
	IF 전후 == "전"
		CALL KOJO_K10_NOON_BEFORE_PLAYER(레밀리아_대상)
	ELSEIF 전후 == "후"
		CALL KOJO_K10_NOON_AFTER_PLAYER(레밀리아_대상)
	ENDIF
ELSEIF 레밀리아구상실대카테고리 == "레밀리아_대상"
	;전문이나 후문인가
	IF 전후 == "전"
		CALL KOJO_K10_NOON_BEFORE_TARGET(레밀리아_대상)
	ELSEIF 전후 == "후"
		CALL KOJO_K10_NOON_AFTER_TARGET(레밀리아_대상)
	ENDIF
ENDIF

RESETCOLOR
RETURN RESULT

