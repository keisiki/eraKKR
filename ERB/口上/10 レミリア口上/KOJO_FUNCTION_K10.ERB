;─────────────────────────────────────── 
;●레밀리아 구상의 사용 가부·초기화
;─────────────────────────────────────── 
@KOJO_ASK_RESET_K10
#DIM SAVE_REDRAW
#DIM SAVE_COLOR
#DIM SAVE_LINE
#DIM 레밀리아

CURRENTREDRAW
SAVE_REDRAW = RESULT
REDRAW 0
GETCOLOR
SAVE_COLOR = RESULT
RESETCOLOR

레밀리아 = NAME_TO_CHARA("레밀리아")

;레밀리아 구상용의 데이터를 초기화
;0=미설정 1=사용하지 않는 2=사용한다
레밀리아_구상데일리입력계사용 = 0
;입력계 데일리 애칭을 가르쳐 용
레밀리아_주인공에게로의호칭 '= ""
레밀리아_주인공에게로의호칭후리가나 '= ""
;입력계 데일리 포상을 주는 용
레밀리아_음어Ｐ '= "그것"
레밀리아_음어Ｖ '= "저기"
레밀리아_음어Ｃ '= "클리"
레밀리아_음어Ａ '= "엉덩이"
레밀리아_음어Ｂ '= "가슴"
;0=미설정 1=저빈도 2=고빈도
레밀리아_음어빈도 = 0
;플레이어측의 첫회 실행 판정（대머리 체크용）
레밀리아_미용사나무첫회 = 0

RESETCOLOR
PRINTL 

PRINTFORML 레밀리아 구상 사용의 설정
CALL KOJO_ASK("사용하지 않는다", "사용한다")
PRINTFORMW 레밀리아 구상의 사용을 설정했다

;사용하지 않는다
IF RESULT == 0
	CFLAG:레밀리아:400 = 1
;사용한다
ELSEIF RESULT == 1
	CFLAG:레밀리아:400 = 2
	PRINTL 
	PRINTFORM 레밀리아 구상 데일리 입력계 사용의 설정
	SETCOLOR 칼라_선택불가
	PRINTFORML  - 당신에게로의 우선 호칭 이네기호의 음어를 입력하는 이벤트
	RESETCOLOR
	CALL KOJO_ASK("사용하지 않는다", "사용한다")
	PRINTFORMW 레밀리아 구상 데일리 입력계 사용을 설정했다
	;사용하지 않는다
	IF RESULT == 0
		레밀리아_구상데일리입력계사용 = 1
	;사용한다
	ELSEIF RESULT == 1
		레밀리아_구상데일리입력계사용 = 2
	ENDIF
ENDIF

PRINTL 

REDRAW SAVE_REDRAW
SETCOLOR SAVE_COLOR
RETURN 0


;─────────────────────────────────────── 
;●레밀리아의 호칭（레밀리아로부터 레밀리아_대상에）
;  사용법:%CALLNAME_K10(레밀리아_대상)%  %CALLNAME_K10(레밀리아_대상, "아")%
;  각서  :제 2 인수에 "아"라고 붙이면
;  후리가나（CSTR:MASTER:6이나 레밀리아_주인공에게로의 호칭 후리가나나 군주등에의 특수 호칭）가 있는 경우
;  대응하는 소쓰기 모음을 적는다（응석부리고 감을 낸다）
;─────────────────────────────────────── 
@CALLNAME_K10(레밀리아_대상, 소쓰기)
#FUNCTIONS
#DIM 레밀리아
#DIM 레밀리아_대상
#DIMS 소쓰기
#DIMS 표시텍스트
#DIMS 표시테키스트후리가나

;리셋트
레밀리아 = NAME_TO_CHARA("레밀리아")
표시텍스트 '= ""

;주인공으로 호칭 지정이 끝난 상태
IF 레밀리아_대상 == MASTER && KDVAR:레밀리아:레밀리아_애칭을가르쳐줘
	표시텍스트 '= 레밀리아_주인공에게로의호칭
	표시테키스트후리가나 '= 레밀리아_주인공에게로의호칭후리가나

;위안중 모브 상대
ELSEIF FLAG:조교모드 == 조교_위안 && CFLAG:(레밀리아_대상):위안모브
	표시텍스트 '= "모두"
	표시테키스트후리가나 '= "모두"

ELSEIF CSTR:레밀리아_대상:99 == "파츄리" 
	표시텍스트 '= "파체"
	표시테키스트후리가나 '= "파체"

ELSEIF CSTR:레밀리아_대상:99 == "레이무" && !IS_LOVER(레밀리아) && !IS_SLAVE(레밀리아)
	표시텍스트 '= "무녀"
	표시테키스트후리가나 '= "무녀"

ELSEIF CSTR:레밀리아_대상:99 == "린노스케" && !IS_LOVER(레밀리아) && !IS_SLAVE(레밀리아)
	표시텍스트 '= "점주"
	표시테키스트후리가나 '= "텐슈"

;그 이외
ELSE
	IF CSTR:레밀리아_대상:99 == "당신"
		IF IS_MALE(레밀리아_대상)
			표시텍스트 '= "당신"
			표시테키스트후리가나 '= "당신"
		ELSE
			표시텍스트 '= "당신"
			표시테키스트후리가나 '= "당신"
		ENDIF
	ELSE
		표시텍스트 '= ANAME(레밀리아_대상)
		IF CSTR:레밀리아_대상:6 != ""
			표시테키스트후리가나 '= CSTR:레밀리아_대상:6
		ELSE
			표시테키스트후리가나 '= ""
		ENDIF
	ENDIF
ENDIF

IF 소쓰기 == "" && 표시테키스트후리가나 != ""
	표시텍스트 '= 표시텍스트 + SMALLSCRIPT(표시테키스트후리가나)
ENDIF

RETURNF 표시텍스트


;─────────────────────────────────────── 
;●레밀리아 구상의 분기 조건 통계
;  IF문을 단축하거나 보기 쉽고하기 위한 조건 통계
;  자주(잘) 사용하는 조건이 증가하면 그때마다 추가
;  조건=알기 쉬운 것 같은 조건명 레밀리아_대상=생략하면 주인공
;  CHECK_K10("안면", 레밀리아_대상)……기존과 취급하는 고정 캐릭터인가, 안면은 있을까의 확인
;  CHECK_K10("함락", 레밀리아_대상)……레밀리아_대상이 주인공이나＆연모계 복종계의 함락이 있는지 확인한다
;  CHECK_K10("홍마관", 레밀리아_대상)……홍마관의 멤버인가 어떤가（기존 또한 호의적 취급）
;  CHECK_K10("활력")……레밀리아의 기력 체력이 있는 또는 레밀리아에 주도권이 있다
;─────────────────────────────────────── 
@CHECK_K10(조건, 레밀리아_대상)
#FUNCTION
#DIMS 조건
#DIM 레밀리아_대상
#DIM 레밀리아

;레밀리아_대상은 미지정이라면 주인공
IF !레밀리아_대상
	레밀리아_대상 = MASTER
ENDIF

레밀리아 = NAME_TO_CHARA("레밀리아")

;0 안면
IF 조건 == "안면"
	;레밀리아_대상이 당신으로 레밀리아와 안면이 있다（안면을 얻는 데일리의 발생도 포함한다）
	IF 레밀리아_대상 == MASTER && CFLAG:레밀리아:안면
		RETURNF 1
	;고정 캐릭터로 홍마관캐릭터의 경우
	ELSEIF ANAME(레밀리아_대상) == "레밀리아" || ANAME(레밀리아_대상) == "파츄리" || ANAME(레밀리아_대상) == "프랑" || ANAME(레밀리아_대상) == "소악마" || ANAME(레밀리아_대상) == "메이링"
		RETURNF 1
	;고정 캐릭터로 레밀리아가 아는 사람으로서 취급할 것 같은 캐릭터의 경우（특수한 부르는 법 따위가 있는 캐릭터）
	ELSEIF ANAME(레밀리아_대상) == "레이무" || ANAME(레밀리아_대상) == "마리사" || ANAME(레밀리아_대상) == "린노스케"
		RETURNF 1
	;그 이외
	ELSE
		RETURNF 0
	ENDIF
ENDIF

;0 함락
IF 조건 == "함락"
	;레밀리아_대상이 당신으로 연모계나 복종계의 함락
	IF 레밀리아_대상 == MASTER && (IS_SLAVE(레밀리아) || IS_LOVER(레밀리아))
		RETURNF 1
	ENDIF
ENDIF

;0홍마관
IF 조건 == "홍마관"
	;레밀리아_대상이 고정 캐릭터로 홍마관캐릭터의 경우（일단 레밀리아 자신도）
	IF ANAME(레밀리아_대상) == "레밀리아" || ANAME(레밀리아_대상) == "파츄리" || ANAME(레밀리아_대상) == "프랑" || ANAME(레밀리아_대상) == "소악마" || ANAME(레밀리아_대상) == "메이링" || ANAME(레밀리아_대상) == "레밀리아"
		RETURNF 1
	ENDIF
ENDIF

;0 활력（기력 체력 1/3이상 또는 주도권 소유）
IF 조건 == "활력"
	IF BASE:레밀리아:기력 > (MAXBASE:레밀리아:기력 / 3) && BASE:레밀리아:체력 > (MAXBASE:레밀리아:체력 / 3)
		RETURNF 1
	ENDIF
	IF IS_INITIATIVE(레밀리아)
		RETURNF 1
	ENDIF
ENDIF

RETURNF 0


;─────────────────────────────────────── 
;●레밀리아의 아야말을 결정한다
;  사용법:꺾쇠 괄호전은 %BREAK_K10("끝")%  그렇지 않으면 %BREAK_K10("안")%
;  각서  :아야말이라고 할까 쉼표·감탄부·하트
;  기분이라든지 쾌락 정도라든지 커맨드종을 봐 「.」붙일까 「!」(이)나 「?」붙일까 결정한다
;  꺾쇠 괄호의 직전 이외는! (이)나? 의 뒤로 전각 스페이스를 들어갈 수 있기 때문에, 안인가 끝인가의 지정이 있다
;  촉음 「읏」를 부록 붙이지 않았다거나 하므로, 원텍스트의 말미에 붙여 버리면 「읏」가 된다
;─────────────────────────────────────── 
@BREAK_K10(위치, 레밀리아_대상)
#FUNCTIONS
#DIMS 위치
#DIMS 단락
#DIMS 표시텍스트
#DIM 레밀리아
#DIM 레밀리아_대상
#DIMS 레밀리아기분
#DIM 레밀리아_최대쾌락부위의값

;레밀리아_대상은 미지정이라면 주인공
IF !레밀리아_대상
	레밀리아_대상 = MASTER
ENDIF

레밀리아 = NAME_TO_CHARA("레밀리아")
레밀리아기분 '= TOSTR_EMOTION(레밀리아)
레밀리아_최대쾌락부위의값 = MAX(PALAM:레밀리아:쾌Ｃ, PALAM:레밀리아:쾌Ｖ, PALAM:레밀리아:쾌Ａ, PALAM:레밀리아:쾌Ｂ, PALAM:레밀리아:쾌Ｍ)

표시텍스트 '= ""

;▼일상 커맨드 실패시
IF TFLAG:18 == -1 || TFLAG:17 == -1
	표시텍스트 '= SPLIT_R("……:::::")
	IF 위치 == "중"
		표시텍스트 '= 표시텍스트 + "。"
	ENDIF

;▼불쾌함
ELSEIF GROUPMATCH(레밀리아기분,"원한","분노","분개","암울","슬픔","우울","광기","공포","겁냄")
	표시텍스트 '= SPLIT_R("……:::::")
	표시텍스트 '= 표시텍스트 + SPLIT_R(":! :::::")
	IF 위치 == "중"
		표시텍스트 '= 표시텍스트 + "。"
	ENDIF

;▼기력 잘라라/취기 한계
ELSEIF TCVAR:레밀리아:52 || TCVAR:레밀리아:53 == 1
	;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
	 IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
		표시텍스트 '= SPLIT_R("……:::::")
		표시텍스트 '= 표시텍스트 + SPLIT_R(":::::")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + "。"
		ENDIF
	;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
	ELSE
		표시텍스트 '= SPLIT_R("……:::::")
		표시텍스트 '= 표시텍스트 + SPLIT_R(":::::")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + SPLIT_R(". :? 　")
		ELSE
			표시텍스트 '= 표시텍스트 + SPLIT_R(":?")
		ENDIF
	ENDIF

;▼체력 잘라라 
ELSEIF TCVAR:레밀리아:51 || (BASE:레밀리아:체력 <= (MAXBASE:레밀리아:체력 / 3))
	IF 레밀리아_최대쾌락부위의값 >= 9000
		표시텍스트 '= SPLIT_R("……:")
		표시텍스트 '= 표시텍스트 + SPLIT_R(":")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + "。"
		ENDIF
	ELSE
		표시텍스트 '= SPLIT_R("……:")
		표시텍스트 '= 표시텍스트 + SPLIT_R(":::::")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + "。"
		ENDIF
	ENDIF

;▼침실에서 레밀리아가 접수
;  접수 화도인가는 NIGHT의 카테고리 분기에서 보존된다
ELSEIF FLAG:우후후플래그 && 레밀리아_레밀리아가접수
	;▽～1500  아……응……
	IF 레밀리아_최대쾌락부위의값 < 1500
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		 IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R("……:")
			표시텍스트 '= 표시텍스트 + SPLIT_R(":::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + "。"
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R("……:")
			표시텍스트 '= 표시텍스트 + SPLIT_R(":::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":?")
			ENDIF
		ENDIF

	;▽1500～5000  아, 우, 아?   ……, 응
	ELSEIF 레밀리아_최대쾌락부위의값 >= 1500 && 레밀리아_최대쾌락부위의값 < 5000
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		 IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R("……:::")
			표시텍스트 '= 표시텍스트 + SPLIT_R("::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + "。"
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R("……:::")
			표시텍스트 '= 표시텍스트 + SPLIT_R("::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":?")
			ENDIF
		ENDIF

	;▽5000～9000  아. 안 됏!   도, 이제(이미/벌써/더는/정말). 는 응?
	ELSEIF 레밀리아_최대쾌락부위의값 >= 5000 && 레밀리아_최대쾌락부위의값 < 9000
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		 IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R(":::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :. :. :! 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":::!")
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R(":")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :. :. :!   :?   :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":::! :? :?")
			ENDIF
		ENDIF

	;▽9000～  가는 우우 우우?   말해 섬우우～!
	ELSE
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		 IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R("--:～:-:……:::::::::::::::")
			표시텍스트 '= 표시텍스트 + SPLIT_R(":::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :! 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":!")
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R("--:～:-:……:::::::::::::::")
			표시텍스트 '= 표시텍스트 + SPLIT_R(":::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :!   :!   :?   :?   :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":! :! :? :? :?")
			ENDIF
		ENDIF
	ENDIF

;▼침실에서 레밀리아가 공격
ELSEIF FLAG:우후후플래그
	;▽～5000
	IF 레밀리아_최대쾌락부위의값 < 5000
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R("……:……:::::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + "。"
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R("……:::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":?")
			ENDIF
		ENDIF

	;▽5000～9000
	ELSEIF 레밀리아_최대쾌락부위의값 >= 5000 && 레밀리아_최대쾌락부위의값 < 9000
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R("……:::::::::")
			IF 위치 == "중"
				표시텍스트 '= "。"
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R(":::::")
			IF 위치 == "중"
				표시텍스트 '= SPLIT_R(". :? 　")
			ELSE
				표시텍스트 '= SPLIT_R(":?")
			ENDIF
		ENDIF

	;▽9000～
	ELSE
		;레밀리아_대상이 주인공이 아닌지, 함락은 아닌, 혹은 포로 스카 촉수계
		IF !CHECK_K10("함락", 레밀리아_대상) || ((SELECTCOM >= 80 && SELECTCOM <= 99) || (SELECTCOM >= 130 && SELECTCOM <= 159) || (SELECTCOM >= 200 && SELECTCOM <= 219))
			표시텍스트 '= SPLIT_R(":::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :. :. :. :. :!")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":::::!")
			ENDIF
		;레밀리아_대상이 주인공 또한 함락, 포로 스카 촉수계는 아니다
		ELSE
			표시텍스트 '= SPLIT_R(":::::")
			IF 위치 == "중"
				표시텍스트 '= 표시텍스트 + SPLIT_R(". :. :. :! :?   :?   :? 　")
			ELSE
				표시텍스트 '= 표시텍스트 + SPLIT_R(":::! :? :? :?")
			ENDIF
		ENDIF
	ENDIF

;▼일상
ELSE
	;레밀리아_대상이 주인공이 아닌지, 함락은 아니다
	IF !CHECK_K10("함락", 레밀리아_대상)
		표시텍스트 '= SPLIT_R("……:::::")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + "。"
		ENDIF
	;레밀리아_대상이 주인공 또한 함락
	ELSE
		표시텍스트 '= SPLIT_R("……:::::")
		IF 위치 == "중"
			표시텍스트 '= 표시텍스트 + SPLIT_R(". :? 　")
		ELSE
			표시텍스트 '= 표시텍스트 + SPLIT_R(":?")
		ENDIF
	ENDIF
ENDIF

RETURNF 표시텍스트

;─────────────────────────────────────── 
;●레밀리아의 음어
;  사용법:%CALL_DIRTY_K10("Ｐ", "아")%
;  각서  :제 2 인수에 "아"를 붙이면 플레이어의 입력한 음어에
;  소쓰기 모음을 적어 응석부리고 음성감을 낸다
;─────────────────────────────────────── 
@CALL_DIRTY_K10(부위, 소쓰기)
#FUNCTIONS
#DIMS 부위
#DIMS 소쓰기
#DIMS 표시텍스트

표시텍스트 '= ""

;로드 데이터용 초기화
IF 레밀리아_음어Ｐ == ""
	레밀리아_음어Ｐ '= "그것"
	레밀리아_음어Ｖ '= "(안)중"
	레밀리아_음어Ｃ '= "클리"
	레밀리아_음어Ａ '= "엉덩이"
	레밀리아_음어Ｂ '= "가슴"
ENDIF

IF 소쓰기 == ""
	IF 부위 == "Ｐ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｐ + SMALLSCRIPT(레밀리아_음어Ｐ)
				CASEELSE
					표시텍스트 '= "그것"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｐ + SMALLSCRIPT(레밀리아_음어Ｐ)
		ENDIF
	ELSEIF 부위 == "Ｖ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｖ + SMALLSCRIPT(레밀리아_음어Ｖ)
				CASEELSE
					표시텍스트 '= "(안)중"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｖ + SMALLSCRIPT(레밀리아_음어Ｖ)
		ENDIF
	ELSEIF 부위 == "Ｃ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｃ + SMALLSCRIPT(레밀리아_음어Ｃ)
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｃ + SMALLSCRIPT(레밀리아_음어Ｃ)
		ENDIF
	ELSEIF 부위 == "Ａ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ａ + SMALLSCRIPT(레밀리아_음어Ｖ)
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ａ + SMALLSCRIPT(레밀리아_음어Ａ)
		ENDIF
	ELSEIF 부위 == "Ｂ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｂ + SMALLSCRIPT(레밀리아_음어Ｂ)
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｂ + SMALLSCRIPT(레밀리아_음어Ｂ)
		ENDIF
	ENDIF
ELSE
	IF 부위 == "Ｐ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｐ
				CASEELSE
					표시텍스트 '= "그것"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｐ
		ENDIF
	ELSEIF 부위 == "Ｖ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｖ
				CASEELSE
					표시텍스트 '= "(안)중"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｖ
		ENDIF
	ELSEIF 부위 == "Ｃ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｃ
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｃ
		ENDIF
	ELSEIF 부위 == "Ａ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ａ
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ａ
		ENDIF
	ELSEIF 부위 == "Ｂ"
		IF 레밀리아_음어빈도 != 2
			SELECTCASE RAND:3
				CASE 1
					표시텍스트 '= 레밀리아_음어Ｂ
				CASEELSE
					표시텍스트 '= "거기"
			ENDSELECT
		ELSE
			표시텍스트 '= 레밀리아_음어Ｂ
		ENDIF
	ENDIF
ENDIF

RETURNF 표시텍스트


