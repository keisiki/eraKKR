;-------------------------------------------------
;「포로 조교」의 말
;-------------------------------------------------

;=================================================
;●개시시의 대사
;=================================================
@KOJO_TRAIN_START_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태라면 말을 표시하지 않는다
IF TALENT:공허
	RETURN
ENDIF

;-------------------------------------------------
;첫회
;-------------------------------------------------
IF CFLAG:202 == 0
	CFLAG:202 = 1
	PRINTFORML 
	;첫대면의 경우
	IF ! CFLAG:안면 ;투옥 첫회
		IF MASTER == NAME_TO_CHARA("묘우렌")
			PRINTFORML 「설마――, 정말로%ANAME(MASTER)%인 것입니까……?!
			PRINTFORMW   그러나 어째서, 왜 당신이 이와 같은 일을?! 거기에…… 왜냐하면[だって] , 당신은 이미……!」
			PRINTFORML 
			PRINTFORML  감옥에 연결될 수 있던%NOUN_SEX(TARGET)%의 놀라움은, 난폭한 숨결을 수반하면서 방을 울리게 해
			PRINTFORMW  숨을 삼킨%ANAME(TARGET)%의, 있을 수 없다고 말하고 싶은 듯한 시선은, 눈을 크게 열어 응시하고 있었다
			PRINTFORML 
			PRINTFORMW  숨길 수가 없었다 큰 낙담
			PRINTFORML 
			PRINTFORML  그 실망은, 정말로 그%ANAME(MASTER)%인가를 의심하면서, 헛들을 길 없는 그 소리와
			PRINTFORMW  극소 변함없는 용모에 타격을 받으면서, 어떻게 변심 하면 이와 같은 일을 할 수 있는지 악문다
			PRINTFORML 
			PRINTFORMW  그렇게 해서 어깨를 떨어뜨리는%ANAME(TARGET)%에 대해,
			PRINTFORML 
			PRINTFORML 「――, 무엇을 말하고 있습니까!? 이 처사의 도대체 어디가 수행이라면 말하는 것입니까?!」
			PRINTFORMW 
			PRINTFORML  믿을 수 없는 말과 행동에 낭패 하는%ANAME(TARGET)%의 울먹이는 소리를 무시해,
			PRINTFORML  덮어씌우려고 하는%ANAME(MASTER)%를 눌러 두는 힘은 어디에도 없었다...
		ELSE
			PRINTFORML 「나에게는――, 이와 같은 장소에 끌려 와 버린 사람들의 원망의 목소리가 들려 옵니다」
			PRINTFORMW 
			PRINTFORMW  쇠사슬에 연결될 수 있던%NOUN_SEX(TARGET)%는, %ANAME(MASTER)%가 방에 들어가자마자 그렇게 단언하면,
			PRINTFORML
			PRINTFORML 「조금이라도 당신에게 양심이 남아 있다면, 이와 같은 비도[非道]인 행동은 그만두어야 합니다
			PRINTFORMW   부디 더 이상, 죄를 거듭하지 말아 주세요……」
			PRINTFORML 
			PRINTFORMW  희미한 동정과 실망을 섞으면서, 억양을 억제한 소리로%ANAME(MASTER)%를 설득하기 시작한 것이었다
			PRINTFORML 
			PRINTFORML  ――날뛴 흔적이 남는 새 상처의 갖가지와 축 무력하게 처진 손목
			PRINTFORMW  끼워지고 있는 수갑의 틈새로부터 들여다 보는 하얀 피부에, 현저하게 푸른 멍이 부상하고 있었다
			PRINTFORML 
			PRINTFORML 「…… 그 손으로 닿자 라고는 생각하지 말아 주세요…… 부처 二의 얼굴도, 세번까지라고 합니다……」
			PRINTFORMW 
			PRINTFORML  고민의 안색에, 그을음 투성이가 된%PRONOUN(TARGET)%의 지체(팔다리와 몸)
			PRINTFORMW  그러나, 그것을 지불할 수도 하지 못하고 몸을 비트는 모습은 요염하다
			PRINTFORML 
			PRINTFORML 「무엇보다 그 같은 일을 되어 입다물고 있을 수 있는 만큼, 나도 어리숙한 사람이 아닙니다…… 이기 때문에」
			PRINTFORMW 
			PRINTFORMW  아직도 움직이지 않는%ANAME(MASTER)%에 초조해 할 수 있던%PRONOUN(TARGET)%의 소리도, 벌이 나쁜 것 같게 날카롭게 날카롭게 할 수 있었던 시선도,
			PRINTFORML 
			PRINTFORML 「!? 그, 그만두세요!」
			PRINTFORMW 
			PRINTFORML  %ANAME(MASTER)%는 무시 해서, 드디어 그 손을 펴고 있었다...
		ENDIF

	;이미 만났던 적이 있는 경우
	ELSE
		PRINTFORML 「나에게는――, 이와 같은 장소에 끌려 와 버린 사람들의 원망의 목소리가 들려 옵니다」
		PRINTFORMW 
		PRINTFORMW  쇠사슬에 연결될 수 있던%NOUN_SEX(TARGET)%는, %ANAME(MASTER)%가 방에 들어가자마자 그렇게 단언하면,
		PRINTFORML
		PRINTFORML 「조금이라도 당신에게 양심의 조각이 남아 있다면, 이와 같은 비도[非道]는 그만두어야 합니다
		PRINTFORMW   그리고 부디 더 이상은…… 죄를 거듭하지 말아 주세요……」
		PRINTFORML 
		PRINTFORMW  희미한 동정과 실망을 섞으면서, 억양을 억제한 소리로%ANAME(MASTER)%를 설득하기 시작한 것이었다
		PRINTFORML 
		PRINTFORML  ――날뛴 흔적이 남는 새 상처의 갖가지와 축 무력하게 처진 손목
		PRINTFORMW  끼워지고 있는 수갑의 틈새로부터 들여다 보는 하얀 피부에, 현저하게 푸른 멍이 부상하고 있었다
		PRINTFORML 
		IF TALENT:연모 || TALENT:연인 ;(의역:그렇지만도 않은 or 권유 접수)
			PRINTFORMW  그리고 아직도 움직이지 않는%ANAME(MASTER)%에 초조해 할 수 있던%PRONOUN(TARGET)%가 소리를 높인다
			PRINTFORML 
			PRINTFORML 「…… 다시 생각해…… 만일 수 있지 않습니까……?」
			PRINTFORMW 
			PRINTFORML  그런%PRONOUN(TARGET)%의 고민의 안색에, 그을음 투성이가 된 지체(팔다리와 몸)
			PRINTFORMW  그러나, 그것을 지불할 수도 하지 못하고 몸을 비트는 모습은 어딘가 요염하다
			PRINTFORML 
			PRINTFORML  ――그리고 눈치챈 희미한 변화
			PRINTFORMW 
			PRINTFORML  조금 땀흘려 빨강뺨. 내리뜨는 눈 십상으로 묻는 시선은, 당황스러움의 색을 숨길 수 있지 않았다
			PRINTFORMW  기분의 나쁨을, 자세를 바꾸었을 때로 보이는 작은 틈새에, 하얀 피부가 부각된다
			PRINTFORML 
			PRINTFORML 「…… 에? 얏…… 기, 기다려 주세요……! 안됩니다…… 아, 안되어…… 안돼……!」
			PRINTFORMW 
			PRINTFORML  끌어당길 수 있도록(듯이) 손을 뻗어 버린%ANAME(MASTER)%는, 그대로%PRONOUN(TARGET)%에 덮어씌웠다...
		ELSE
			PRINTFORML 「?! 접근하지 말아 주세요!」
			PRINTFORMW 
			PRINTFORMW  반사적으로 움찔하는%PRONOUN(TARGET)%의 눈에 켜지는 빛이라고 화내
			PRINTFORML 
			PRINTFORML 「아무리 낯익은 사이 그렇다고 해서 그것을 허락할 수 있을 정도의 관용을 가지고 있는 것이 아닙니다!」
			PRINTFORMW 
			PRINTFORML  찔러넣는 빛에 반사해 빛나는 물방울은, 공포의 색을 희미하게 눈동자에 배이게 하고 있다
			PRINTFORMW  %ANAME(MASTER)%가 가까워질 때, 뒤로 물러나는%PRONOUN(TARGET)%에 맞추어, 쟈라리와 쇠사슬이 울린다
			PRINTFORML 
			PRINTFORML 「그, 그만두세요! 다시 생각하세요……! 그…… 그만두어라고……!」
			PRINTFORMW 
			PRINTFORML  떨리는%PRONOUN(TARGET)%가 거칠게 하는 소리를 무시해, %ANAME(MASTER)%는 그 손을 펴고 있었다...
		ENDIF
	ENDIF
	PRINTFORMW 
;-------------------------------------------------
;개시시(1회만)
;-------------------------------------------------
;연모 또는 복종을 획득했다
ELSEIF (CFLAG:202 < 3) && ! (CFLAG:200 >= 2) 
	IF TALENT:연모
		CFLAG:202 = 3
		PRINTFORML 
		PRINTFORML 「…… 또…… 합니까……?」
		PRINTFORMW 
		PRINTFORML  %ANAME(MASTER)%가 방으로 들어가든지, 어딘가 복잡할 것 같은 얼굴로%PRONOUN(TARGET)%는 말한다
		PRINTFORMW  그것은, 버튼을 엇갈려져 버린 같은 것. 희롱해지고 계속한 결과의, 착각의 끝의 변심
		PRINTFORML 
		PRINTFORML  그 위화감에, 삐끄덩 하고눈썹이 은밀하게 움직인다
		PRINTFORMW 
		PRINTFORML  조금 땀흘려 빨강뺨. 내리뜨는 눈 십상으로 묻는 시선은, 당황스러움의 색에 섞인 뜨거운 시선
		PRINTFORMW  기분의 나쁨을, 자세를 바꾸었을 때로 보이는 작은 틈새에, 하얀 피부가 부각된다
		PRINTFORML  
		PRINTFORML  접하려고 한 순간력 없게 샌 한숨에, 새삼스러운 같게 확 한다
		PRINTFORMW  이 상황하로 정상적인 판단을 잃어, 그 이름은 이미, 진흙 투성이가 되어 빠지고 있다
		PRINTFORML  
		PRINTFORML 「…… 나, 당신이…… 부탁이니까, 이제 이런 일은 그만두어 관――, …… 이……」
		PRINTFORMW 
		PRINTFORML  충동적으로 반사적인%PRONOUN(TARGET)%의 말에 "의미는 없고,
		PRINTFORML  들여다 보도록(듯이) 가까이서 얼굴을 맞댄 순간――녹는 여자가 거기에 있었다...
		PRINTFORMW 

	ELSEIF TALENT:복종
		CFLAG:202 = 3
		PRINTFORML 
		PRINTFORML 「…… 하, 지…… 지금, 준비 합니다……」
		PRINTFORMW 
		PRINTFORML  %ANAME(MASTER)%가 방으로 들어가든지, 어딘가 복잡할 것 같은 얼굴로%PRONOUN(TARGET)%는 말한다
		PRINTFORMW  그것은, 희롱해지고 계속한 결과의 변심. 조교의 끝에 예의범절을 가르쳐진, 저항할 수 없는 상하 관계
		PRINTFORML 
		PRINTFORMW  팔에 연결되어지고 있던 쇠사슬은 어느덧 목에――자유롭게 된 양손을 사용해, 신체를 작게 숨기고 있다
		PRINTFORML 
		PRINTFORML  그러나, 눈치챈 그 위화감
		PRINTFORMW 
		PRINTFORML  조금 땀흘려 빨강뺨. 내리뜨는 눈 십상으로 묻는 시선은, 칙칙한 색에 미덥지 않은 시선
		PRINTFORML  기분의 나쁨을 속여 있는 것인가라고 생각하면, 조금 떨리도록(듯이) 움직이는 손가락끝은,
		PRINTFORMW  방울져 떨어지는 것 같은 물의 소리를 울리게 해로 하고 있다
		PRINTFORML  
		PRINTFORML  그것을 지적한 순간, 샌 한숨에 새삼스러운 같게 확 한다
		PRINTFORMW  정상적인 판단은 완전히 없어져 그 이름은 이미, 진흙 투성이가 되어 빠지고 있다
		PRINTFORML  
		PRINTFORML 「…… 읏……」
		PRINTFORMW 
		PRINTFORML  작게 입술을 깨물어 참고 견디는%PRONOUN(TARGET)%에 저항의 의사는 이미 얇고,
		PRINTFORML  들여다 보도록(듯이) 얼굴을 맞댄 순간, %PRONOUN(TARGET)%의 얼굴은 절망과 쾌락에 녹아 시작하고 있었다...
		PRINTFORMW 
	ENDIF
;의존도가 300이상이 되었다
ELSEIF CFLAG:202 < 2 && CFLAG:의존도 >= 800
	CFLAG:202 = 2
	PRINTFORML 
	PRINTFORML 「…… 부탁…… 합니다…… 이런 일은 이제…… 그만두어……」
	PRINTFORMW 

ELSEIF CFLAG:202 < 2 && CFLAG:의존도 >= 300
	CFLAG:202 = 2
	PRINTFORML 
	PRINTFORML 「…… 어째서…… 이런 일을……」
	PRINTFORMW 

;-------------------------------------------------
;개시시(2회째 이후)
;-------------------------------------------------
;연모
ELSEIF TALENT:연모 || TALENT:연인
	PRINTFORML 
	SELECTCASE RAND:5
		CASE 0
			PRINTFORML 「아, 안됩니닷……! 이런 일, 잘못되어 있습니다……! 잘못되어 있습니다……!」
		CASE 1
			PRINTFORML 「…… 어째서…… 어째서 이런 일에……」
		CASE 2
			PRINTFORML 「…… 부탁이니까…… 이제, 그만두어……」
		CASE 3
			PRINTFORML 「――, 다릅니다! 기꺼이라고 없습니다! 기쁘고 같은거 없습니다……」
		CASE 4
			PRINTFORML 「――아…… 아, 아아앗, 다, 다, 다릅니다! 다릅니다! 그런 취미는 없습니다!」
	ENDSELECT
	PRINTFORMW 

;복종
ELSEIF TALENT:복종 || TALENT:낙인
	PRINTFORML 
	SELECTCASE RAND:5
		CASE 0
			PRINTFORML 「…… 이런 일, 잘못되어 있는데…… 잘못되어 있을 것인데……」
		CASE 1
			PRINTFORML 「…… 하, 이…… 부디……」
		CASE 2
			PRINTFORML 「…… 그, 그런…… 젖어라고…… 없습니다……」
		CASE 3
			PRINTFORML 「…… 기대, 는…… 하지 않았습니다……」
		CASE 4
			PRINTFORML 「히, 우!? 있었닷……! 그, 그만두어라고…… 엉덩이, 부어, 읏!」
	ENDSELECT
	PRINTFORMW 

;의존도
ELSEIF CFLAG:의존도 >= 800
	PRINTFORML 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML 「…… 부탁 합니다…… 이제…… 그만두어……」
		CASE 1
			PRINTFORML 「…… 아픈 것은………… 아니……」
		CASE 2
			PRINTFORML 「…… 죄송합니다…… 내가 안 되었던 것…… 이기 때문에……」
	ENDSELECT
	PRINTFORMW 
;그 이외
ELSE
	PRINTFORML 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML 「……………………」
		CASE 1
			PRINTFORML 「…… 머지않아 부처 벌이, 아니요…… 내가 내립시다……」
		CASE 2
			PRINTFORML 「………… 최악입니다…………」
	ENDSELECT
	PRINTFORMW 
ENDIF

;=================================================
;●종료시의 대사
;=================================================
@KOJO_TRAIN_END_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태라면 말을 표시하지 않는다
IF TALENT:공허
	RETURN
ENDIF

;-------------------------------------------------
;행동 불능의 경우
;-------------------------------------------------
;술에 취함에 의한 행동 불능
;IF TCVAR:53 == 1
	;PRINTFORMW 
;쾌감의 별로 기절
IF TCVAR:52
	PRINTFORML 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML  %ANAME(TARGET)%는, 땀 투성이가 된 신체를 내던진 채로, 공허한 눈동자로 쾌락에 빠지고 있었다...
		CASE 1
			PRINTFORML  허리로부터 붕괴되어 버린%ANAME(TARGET)%는, 전신을 진동시키고 난폭한 한숨을 반복하고 있었다...
		CASE 2
			PRINTFORML  허리를 삐걱삐걱 진동시키고 붕괴되는%ANAME(TARGET)%는, 쾌감의 모두에 의식을 손놓아 버렸다...
	ENDSELECT
	PRINTFORMW 

	;얀데체크
	SIF TALENT:정실 && CFLAG:302
		CALL AFTER_CHECK_YANDERE_K77

;피로에 의한 행동 불능
ELSEIF TCVAR:51
	PRINTFORML 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML 「…… 하앗…… 하…… 하아…… 아, 아……」
		CASE 1
			PRINTFORML 「…… 하앗…… 아앗…… 응, 웃…… 하앗……」
		CASE 2
			PRINTFORML 「…… 하아아…… 하아…… 하아……」
	ENDSELECT
	PRINTFORML 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML  땀 투성이가 된 신체를 내던진 채로의%ANAME(TARGET)%는, 공허한 눈동자로 쾌락에 빠지고 있었다...
		CASE 1
			PRINTFORML  허리로부터 붕괴되어 버린%ANAME(TARGET)%는 전신을 진동시켜 난폭한 한숨을 반복하고 있었다...
		CASE 2
			PRINTFORML  허리를 삐걱삐걱 진동시키고 붕괴되는%ANAME(TARGET)%는, 쾌감의 모두에 의식을 손놓아 버렸다...
	ENDSELECT
	;얀데체크
	SIF TALENT:정실 && CFLAG:302
		CALL AFTER_CHECK_YANDERE_K77
	PRINTFORMW 
ENDIF

;-------------------------------------------------
;종료시(1회만)
;-------------------------------------------------
;처음으로 의존도가 300이상이 되었다
IF CFLAG:203 < 2 && CFLAG:의존도 >= 300
	CFLAG:203 = 2
	;행동 불능이라면 플래그만 세워 표시는 하지 않는다
	IF ! (TCVAR:51 || TCVAR:52 || TCVAR:53)
		PRINTFORML 
		IF IS_LOVER(뱌쿠렌)

		ELSEIF IS_SLAVE(뱌쿠렌)
		
		ELSE
			PRINTFORML 「…… 최악입니다…… 이런…… 이런 일을 하다니……」
		ENDIF
		PRINTFORMW 
	ENDIF

;첫회
ELSEIF CFLAG:203 < 1
	CFLAG:203 = 1
	;행동 불능이라면 플래그만 세워 표시는 하지 않는다
	IF ! (TCVAR:51 || TCVAR:52 || TCVAR:53)
		PRINTFORML 
		IF IS_LOVER(뱌쿠렌)
			PRINTFORML 「조금, 경멸 했던…… 이런 곳에서…… 하다니……」
		ELSEIF IS_SLAVE(뱌쿠렌)
			PRINTFORML 「…… 또………… 여기에서, 이군요……」
		ELSE
			PRINTFORML 「…… 최악입니다…… 이런…… 이런 일을 하다니……」
		ENDIF
		PRINTFORMW 
	ENDIF

;-------------------------------------------------
;종료시(2회째 이후)
;-------------------------------------------------
;행동 불능이라면 비표시
ELSEIF ! (TCVAR:51 || TCVAR:52 || TCVAR:53)
	;연모 or 복종
	IF IS_LOVER(뱌쿠렌)
		SELECTCASE RAND:4
			CASE 0
				PRINTFORML 「빨리 여기에서 내 주면, 기쁜 것입니다만…… 혹시, 가망이 없습니까?」
			CASE 1
				PRINTFORML 「익숙해지면 이것도, 한가지(하나)의 상황인 것일지도 모릅니다만…… 나에게 이 손의 취향은 없어요?」
			CASE 2
				PRINTFORML 「…… 좀 더, 이대로 시켜 주세요…… 여기는 단지, 외로운 것뿐입니다……」
			CASE 3
				PRINTFORML 「저…… 나를 여기에 두고 갈 생각입니까?」
		ENDSELECT
	ELSEIF IS_SLAVE(뱌쿠렌)
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「…… 몸이, 더러워져 버리고 있습니다…… 지금, 내가 닦네요……」
			CASE 1
				PRINTFORML 「…… 하, 이…… 또, 부디 오셔 주십시오……」
			CASE 2
				PRINTFORML 「…………」
		ENDSELECT

	;그 이외
	ELSE
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML 「…… 기분은 풀렸습니까? 그렇다면, 이제…… 여기에는 오지 말아 주세요……」
			CASE 1
				PRINTFORML 「…… 이런 일…… 언제까지라도 계속되지 않아요…… 그렇게 생각하지 않습니까?」
			CASE 2
				PRINTFORML 「…… 돌아가…… 빨리, 돌아가 주세요……」
		ENDSELECT
	ENDIF
ENDIF

;=================================================
;●커맨드 실행전(이 캐릭터가 타겟측 때)
;※지문이 표시되기 전에 실행된다. 반환값에 1을 설정하는(RETURN 1)와 지문이 컷 된다
;  필요에 따라서 KOJO_COM_TARGET 대신에 사용한다
;=================================================
@KOJO_COM_BEFORE_TARGET_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
SIF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0

;-------------------------------------------------
;키스
;-------------------------------------------------
IF SELECTCOM == 20
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;짝 맞추기 놀이
;-------------------------------------------------
IF SELECTCOM == 21
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77

	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;쌍두 바이브래이터
;-------------------------------------------------
IF SELECTCOM == 22
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77

	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;애무(타겟)
;-------------------------------------------------
IF SELECTCOM == 0
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;가슴 애무(타겟)
;-------------------------------------------------
IF SELECTCOM == 1
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;쿠니(타겟)
;-------------------------------------------------
IF SELECTCOM == 2
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;손가락 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 3
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 애무(타겟)
;-------------------------------------------------
IF SELECTCOM == 4
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;거품 춤(타겟)
;-------------------------------------------------
IF SELECTCOM == 5
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 6
	;처음
	IF IS_COM_FIRST()
		;처녀
		IF TALENT:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 Ａ삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 7
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 빨고(타겟)
;-------------------------------------------------
IF SELECTCOM == 8
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;유두 들이마셔(타겟)
;-------------------------------------------------
IF SELECTCOM == 9
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;손 애무(타겟)
;-------------------------------------------------
IF SELECTCOM == 10
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;페라(타겟)
;-------------------------------------------------
IF SELECTCOM == 11
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;파이즈리(젖치기)(타겟)
;-------------------------------------------------
IF SELECTCOM == 12
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;가랑이(타겟)
;-------------------------------------------------
IF SELECTCOM == 13
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;풋잡(타겟)
;-------------------------------------------------
IF SELECTCOM == 14
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;엉덩이 코키(타겟)
;-------------------------------------------------
IF SELECTCOM == 15
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;오나호코키(타겟)
;-------------------------------------------------
IF SELECTCOM == 16
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 코키(타겟)
;-------------------------------------------------
IF SELECTCOM == 17
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;정상위(타겟)
;-------------------------------------------------
IF SELECTCOM == 30
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;후배위(타겟)
;-------------------------------------------------
IF SELECTCOM == 31
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;대면좌위(타겟)
;-------------------------------------------------
IF SELECTCOM == 32
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;배면좌위(타겟)
;-------------------------------------------------
IF SELECTCOM == 33
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;승마위(타겟)
;-------------------------------------------------
IF SELECTCOM == 34
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;배면 승마위(타겟)
;-------------------------------------------------
IF SELECTCOM == 35
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;2 개 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 36
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｖ확장경험 50이상
			IF EXP:Ｖ확장경험 >= 50
				;PRINTFORMW 
			;Ｖ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｖ확장경험 50이상
			IF EXP:Ｖ확장경험 >= 50
				;PRINTFORMW 
			;Ｖ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｖ확장경험 50이상
			IF EXP:Ｖ확장경험 >= 50
				;PRINTFORMW 
			;Ｖ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;측위(타겟)
;-------------------------------------------------
IF SELECTCOM == 37
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;굴곡정도(타겟)
;-------------------------------------------------
IF SELECTCOM == 38
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;역에서 파는 도시락(타겟)
;-------------------------------------------------
IF SELECTCOM == 39
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;배면 역에서 파는 도시락(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 52
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;서 백(타겟)
;-------------------------------------------------
IF SELECTCOM == 160
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A정상위(타겟)
;-------------------------------------------------
IF SELECTCOM == 40
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A후배위(타겟)
;-------------------------------------------------
IF SELECTCOM == 41
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A대면좌위(타겟)
;-------------------------------------------------
IF SELECTCOM == 42
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A배면좌위(타겟)
;-------------------------------------------------
IF SELECTCOM == 43
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A승마위(타겟)
;-------------------------------------------------
IF SELECTCOM == 44
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A배면 승마위(타겟)
;-------------------------------------------------
IF SELECTCOM == 45
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A측위(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 47
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A굴곡정도(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 48
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A역에서 파는 도시락(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 49
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A배면 역에서 파는 도시락(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 53
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A 서 백(타겟·되는 측)
;-------------------------------------------------
IF SELECTCOM == 161
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A2 개 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 46
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ확장경험 50이상
			IF EXP:Ａ확장경험 >= 50
				;PRINTFORMW 
			;Ａ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ확장경험 50이상
			IF EXP:Ａ확장경험 >= 50
				;PRINTFORMW 
			;Ａ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ확장경험 50이상
			IF EXP:Ａ확장경험 >= 50
				;PRINTFORMW 
			;Ａ확장경험 50 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;페니반 장착(타겟)
;-------------------------------------------------
IF SELECTCOM == 50
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;술치육[恥肉]륜(타겟)
;-------------------------------------------------
IF SELECTCOM == 51
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;차례로 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 55
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;차례로 Ａ삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 56
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;번갈아 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 57
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;번갈아 Ａ삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 58
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;로터(타겟)
;-------------------------------------------------
IF SELECTCOM == 60
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;바이브래이터(타겟)
;-------------------------------------------------
IF SELECTCOM == 61
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 바이브래이터(타겟)
;-------------------------------------------------
IF SELECTCOM == 62
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;구리 캡(타겟)
;-------------------------------------------------
IF SELECTCOM == 63
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｃ감 Lv3 이상
			IF ABL:Ｃ감 >= 3
				;PRINTFORMW 
			;Ｃ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｃ감 Lv3 이상
			IF ABL:Ｃ감 >= 3
				;PRINTFORMW 
			;Ｃ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｃ감 Lv3 이상
			IF ABL:Ｃ감 >= 3
				;PRINTFORMW 
			;Ｃ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;니프르캐프(타겟)
;-------------------------------------------------
IF SELECTCOM == 64
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｂ감 Lv3 이상
			IF ABL:Ｂ감 >= 3
				;PRINTFORMW 
			;Ｂ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｂ감 Lv3 이상
			IF ABL:Ｂ감 >= 3
				;PRINTFORMW 
			;Ｂ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｂ감 Lv3 이상
			IF ABL:Ｂ감 >= 3
				;PRINTFORMW 
			;Ｂ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ｖ확장 벌룬(타겟)
;-------------------------------------------------
IF SELECTCOM == 65
	;처음
	IF IS_COM_FIRST()
		;처녀
		IF TALENT:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｖ감 Lv3 이상
			IF ABL:Ｖ감 >= 3
				;PRINTFORMW 
			;Ｖ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ확장 벌룬(타겟)
;-------------------------------------------------
IF SELECTCOM == 66
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;로션(타겟)
;-------------------------------------------------
IF SELECTCOM == 70
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;미약(타겟)
;-------------------------------------------------
IF SELECTCOM == 71
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;배란유발제(타겟)
;-------------------------------------------------
IF SELECTCOM == 72
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;절륜환(타겟)
;-------------------------------------------------
IF SELECTCOM == 73
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;마약(타겟)
;-------------------------------------------------
IF SELECTCOM == 74
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;콘돔(타겟)
;-------------------------------------------------
IF SELECTCOM == 75
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;도원향기(타겟)
;-------------------------------------------------
IF SELECTCOM == 76
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;위협한다(타겟)
;-------------------------------------------------
IF SELECTCOM == 80
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;스팽킹(타겟)
;-------------------------------------------------
IF SELECTCOM == 81
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;채찍(타겟)
;-------------------------------------------------
IF SELECTCOM == 82
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;바늘(타겟)
;-------------------------------------------------
IF SELECTCOM == 83
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;눈가리개(타겟)
;-------------------------------------------------
IF SELECTCOM == 84
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;줄(타겟)
;-------------------------------------------------
IF SELECTCOM == 85
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;입마개(타겟)
;-------------------------------------------------
IF SELECTCOM == 86
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;피스트 성교(타겟)
;-------------------------------------------------
IF SELECTCOM == 87
	;처음
	IF IS_COM_FIRST()
		;처녀
		IF TALENT:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ｖ감 Lv5 이상
			IF ABL:Ｖ감 >= 5
				;PRINTFORMW 
			;Ｖ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ｖ감 Lv5 이상
			IF ABL:Ｖ감 >= 5
				;PRINTFORMW 
			;Ｖ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ｖ감 Lv5 이상
			IF ABL:Ｖ감 >= 5
				;PRINTFORMW 
			;Ｖ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 피스트(타겟)
;-------------------------------------------------
IF SELECTCOM == 88
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ감 Lv5 이상
			IF ABL:Ａ감 >= 5
				;PRINTFORMW 
			;Ａ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ감 Lv5 이상
			IF ABL:Ａ감 >= 5
				;PRINTFORMW 
			;Ａ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ감 Lv5 이상
			IF ABL:Ａ감 >= 5
				;PRINTFORMW 
			;Ａ감 Lv5 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;손바닥 치기(타겟)
;-------------------------------------------------
IF SELECTCOM == 89
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;이라마치오(타겟)
;-------------------------------------------------
IF SELECTCOM == 90
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;피아싱(타겟)
;-------------------------------------------------
IF SELECTCOM == 91
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;타투(타겟)
;-------------------------------------------------
IF SELECTCOM == 92
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;코훅크(타겟)
;-------------------------------------------------
IF SELECTCOM == 93
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;목 조르기(타겟)
;-------------------------------------------------
IF SELECTCOM == 94
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;배빵(타겟)
;-------------------------------------------------
IF SELECTCOM == 97
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;원격 조교(타겟)
;-------------------------------------------------
IF SELECTCOM == 98
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;자위(타겟)
;-------------------------------------------------
IF SELECTCOM == 100
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;안면 승마(타겟)
;-------------------------------------------------
IF SELECTCOM == 101
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;플레이어가 질이 없다(구슬 빨고)
			IF ! HAS_VAGINA(MPLY:0)
				;PRINTFORMW 
			;플레이어에 질이 있다(비부[秘部] 빨고)
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;플레이어가 질이 없다(구슬 빨고)
			IF ! HAS_VAGINA(MPLY:0)
				;PRINTFORMW 
			;플레이어에 질이 있다(비부[秘部] 빨고)
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;플레이어가 질이 없다(구슬 빨고)
			IF ! HAS_VAGINA(MPLY:0)
				;PRINTFORMW 
			;플레이어에 질이 있다(비부[秘部] 빨고)
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;플레이어가 질이 없다(구슬 빨고)
			IF ! HAS_VAGINA(MPLY:0)
				;PRINTFORMW 
			;플레이어에 질이 있다(비부[秘部] 빨고)
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;플레이어가 질이 없다(구슬 빨고)
			IF ! HAS_VAGINA(MPLY:0)
				;PRINTFORMW 
			;플레이어에 질이 있다(비부[秘部] 빨고)
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;A안면 승마(타겟)
;-------------------------------------------------
IF SELECTCOM == 102
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;전기 안마(타겟)
;-------------------------------------------------
IF SELECTCOM == 103
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;다리를 빤다(타겟)
;-------------------------------------------------
IF SELECTCOM == 104
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;귀두 비난하는 사람 코키(타겟)
;-------------------------------------------------
IF SELECTCOM == 106
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;귀두 고문 페라(타겟)
;-------------------------------------------------
IF SELECTCOM == 107
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_TARGET_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 셀프 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 108
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 셀프 Ａ삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 109
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;땅에 엎드려 조아림(타겟)
;-------------------------------------------------
IF SELECTCOM == 110
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;졸라댐(타겟)
;-------------------------------------------------
IF SELECTCOM == 111
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;노출 플레이(타겟)
;-------------------------------------------------
IF SELECTCOM == 112
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;음모 관리(타겟)
;-------------------------------------------------
IF SELECTCOM == 113
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;신체에 낙서(타겟)
;-------------------------------------------------
IF SELECTCOM == 114
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;소변보기 한다(타겟)
;-------------------------------------------------
IF SELECTCOM == 130
	PRINTFORML 
	;처음
	IF IS_COM_FIRST()
		
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;오줌을 건다(타겟)
;-------------------------------------------------
IF SELECTCOM == 131
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;입안 소변보기(타겟)
;-------------------------------------------------
IF SELECTCOM == 132
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;이뇨제(타겟)
;-------------------------------------------------
IF SELECTCOM == 140
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;관장(타겟)
;-------------------------------------------------
IF SELECTCOM == 141
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;관장＆플러그(타겟)
;-------------------------------------------------
IF SELECTCOM == 142
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;방귀 한다(타겟)
;-------------------------------------------------
IF SELECTCOM == 150
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;안기방귀(타겟)
;-------------------------------------------------
IF SELECTCOM == 151
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 소환(타겟)
;-------------------------------------------------
IF SELECTCOM == 200
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 201
	;처음
	IF IS_COM_FIRST()
		;처녀
		IF TALENT:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 Ａ삽입(타겟)
;-------------------------------------------------
IF SELECTCOM == 202
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 클리 꾸짖고(타겟)
;-------------------------------------------------
IF SELECTCOM == 203
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수가슴 애무(타겟)
;-------------------------------------------------
IF SELECTCOM == 204
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 오나홀(타겟)
;-------------------------------------------------
IF SELECTCOM == 205
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 코키(타겟)
;-------------------------------------------------
IF SELECTCOM == 206
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 착유(타겟)
;-------------------------------------------------
IF SELECTCOM == 207
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 긴박(타겟)
;-------------------------------------------------
IF SELECTCOM == 208
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수구욕(타겟)
;-------------------------------------------------
IF SELECTCOM == 210
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 자위(타겟)
;-------------------------------------------------
IF SELECTCOM == 211
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 산란(타겟)
;-------------------------------------------------
IF SELECTCOM == 212
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 A산란(타겟)
;-------------------------------------------------
IF SELECTCOM == 213
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;아무것도 하지 않는다(타겟)
;-------------------------------------------------
IF SELECTCOM == 292
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;충성을 맹세하게 한다(타겟)
;-------------------------------------------------
IF SELECTCOM == 299
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;납치(타겟)
;-------------------------------------------------
IF SELECTCOM == 555
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF
RETURN 0

;=================================================
;●커맨드 실행전(이 캐릭터가 플레이어측 때)
;※지문이 표시되기 전에 실행된다. 반환값에 1을 설정하는(RETURN 1)와 지문이 컷 된다
;  필요에 따라서 KOJO_COM_PLAYER 대신에 사용한다
;=================================================
@KOJO_COM_BEFORE_PLAYER_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0
ENDIF

;-------------------------------------------------
;키스
;-------------------------------------------------
IF SELECTCOM == 20
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;짝 맞추기 놀이
;-------------------------------------------------
IF SELECTCOM == 21
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;쌍두 바이브래이터
;-------------------------------------------------
IF SELECTCOM == 22
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;애무(플레이어)
;-------------------------------------------------
IF SELECTCOM == 0
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;가슴 애무(플레이어)
;-------------------------------------------------
IF SELECTCOM == 1
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;쿠니(플레이어)
;-------------------------------------------------
IF SELECTCOM == 2
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;손가락 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 3
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;애널 애무(플레이어)
;-------------------------------------------------
IF SELECTCOM == 4
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;거품 춤(플레이어)
;-------------------------------------------------
IF SELECTCOM == 5
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;꼬리 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 6
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;꼬리 Ａ삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 7
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;애널 빨고(플레이어)
;-------------------------------------------------
IF SELECTCOM == 8
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;유두 들이마셔(플레이어)
;-------------------------------------------------
IF SELECTCOM == 9
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;손 애무(플레이어)
;-------------------------------------------------
IF SELECTCOM == 10
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;페라(플레이어)
;-------------------------------------------------
IF SELECTCOM == 11
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;파이즈리(젖치기)(플레이어)
;-------------------------------------------------
IF SELECTCOM == 12
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;가랑이(플레이어)
;-------------------------------------------------
IF SELECTCOM == 13
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;풋잡(플레이어)
;-------------------------------------------------
IF SELECTCOM == 14
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;엉덩이 코키(플레이어)
;-------------------------------------------------
IF SELECTCOM == 15
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;오나호코키(플레이어)
;-------------------------------------------------
IF SELECTCOM == 16
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 코키(플레이어)
;-------------------------------------------------
IF SELECTCOM == 17
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;정상위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 30
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;후배위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 31
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;대면좌위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 32
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;배면좌위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 33
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;승마위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 34
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;배면 승마위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 35
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;2 개 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 36
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;측위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 37
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;굴곡정도(플레이어)
;-------------------------------------------------
IF SELECTCOM == 38
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;역에서 파는 도시락(플레이어)
;-------------------------------------------------
IF SELECTCOM == 39
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;배면 역에서 파는 도시락(플레이어·되는 측)
;-------------------------------------------------
IF SELECTCOM == 52
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;서 백(플레이어)
;-------------------------------------------------
IF SELECTCOM == 160
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A정상위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 40
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A후배위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 41
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A대면좌위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 42
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A배면좌위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 43
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A승마위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 44
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A배면 승마위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 45
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;A2 개 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 46
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;A측위(플레이어·하는 측)
;-------------------------------------------------
IF SELECTCOM == 47
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A굴곡정도(플레이어·하는 측)
;-------------------------------------------------
IF SELECTCOM == 48
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A역에서 파는 도시락(플레이어·하는 측)
;-------------------------------------------------
IF SELECTCOM == 49
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A배면 역에서 파는 도시락(플레이어·하는 측)
;-------------------------------------------------
IF SELECTCOM == 53
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;A 서 백(플레이어·하는 측)
;-------------------------------------------------
IF SELECTCOM == 161
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;페니반 장착(플레이어)
;-------------------------------------------------
IF SELECTCOM == 50
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;술치육[恥肉]륜(플레이어)
;-------------------------------------------------
IF SELECTCOM == 51
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;차례로 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 55
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;차례로 Ａ삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 56
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;번갈아 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 57
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;번갈아 Ａ삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 58
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;로터(플레이어)
;-------------------------------------------------
IF SELECTCOM == 60
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;바이브래이터(플레이어)
;-------------------------------------------------
IF SELECTCOM == 61
	;처음
	IF IS_COM_FIRST()
		;주인공이 처녀
		IF TALENT:MASTER:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;주인공이 비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 바이브래이터(플레이어)
;-------------------------------------------------
IF SELECTCOM == 62
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;구리 캡(플레이어)
;-------------------------------------------------
IF SELECTCOM == 63
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;니프르캐프(플레이어)
;-------------------------------------------------
IF SELECTCOM == 64
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ｖ확장 벌룬(플레이어)
;-------------------------------------------------
IF SELECTCOM == 65
	;처음
	IF IS_COM_FIRST()
		;주인공이 처녀
		IF TALENT:MASTER:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;주인공이 비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ확장 벌룬(플레이어)
;-------------------------------------------------
IF SELECTCOM == 66
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;로션(플레이어)
;-------------------------------------------------
IF SELECTCOM == 70
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;미약(플레이어)
;-------------------------------------------------
IF SELECTCOM == 71
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;배란유발제(플레이어)
;-------------------------------------------------
IF SELECTCOM == 72
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;절륜환(플레이어)
;-------------------------------------------------
IF SELECTCOM == 73
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;마약(플레이어)
;-------------------------------------------------
IF SELECTCOM == 74
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;콘돔(플레이어)
;-------------------------------------------------
IF SELECTCOM == 75
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;도원향기(플레이어)
;-------------------------------------------------
IF SELECTCOM == 76
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;위협한다(플레이어)
;-------------------------------------------------
;실행 불가

;-------------------------------------------------
;스팽킹(플레이어)
;-------------------------------------------------
IF SELECTCOM == 81
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;채찍(플레이어)
;-------------------------------------------------
IF SELECTCOM == 82
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;바늘(플레이어)
;-------------------------------------------------
IF SELECTCOM == 83
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;눈가리개(플레이어)
;-------------------------------------------------
IF SELECTCOM == 84
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;줄(플레이어)
;-------------------------------------------------
IF SELECTCOM == 85
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;입마개(플레이어)
;-------------------------------------------------
IF SELECTCOM == 86
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;피스트 성교(플레이어)
;-------------------------------------------------
IF SELECTCOM == 87
	;처음
	IF IS_COM_FIRST()
		;주인공이 처녀
		IF TALENT:MASTER:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;주인공이 비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;애널 피스트(플레이어)
;-------------------------------------------------
IF SELECTCOM == 88
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;손바닥 치기(플레이어)
;-------------------------------------------------
IF SELECTCOM == 89
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;이라마치오(플레이어)
;-------------------------------------------------
IF SELECTCOM == 90
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;피아싱(플레이어)
;-------------------------------------------------
IF SELECTCOM == 91
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;타투(플레이어)
;-------------------------------------------------
IF SELECTCOM == 92
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			IF GETBIT(TALENT:음란계, 소질_음란_마조)
				;PRINTFORMW 
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;코훅크(플레이어)
;-------------------------------------------------
IF SELECTCOM == 93
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;목 조르기(플레이어)
;-------------------------------------------------
IF SELECTCOM == 94
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;배빵(플레이어)
;-------------------------------------------------
IF SELECTCOM == 97
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;원격 조교(플레이어)
;-------------------------------------------------
IF SELECTCOM == 98
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;자위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 100
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF
;-------------------------------------------------
;안면 승마(플레이어)
;-------------------------------------------------
IF SELECTCOM == 101
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;A안면 승마(플레이어)
;-------------------------------------------------
IF SELECTCOM == 102
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;Ａ감 Lv3 이상
			IF ABL:Ａ감 >= 3
				;PRINTFORMW 
			;Ａ감 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;전기 안마(플레이어)
;-------------------------------------------------
IF SELECTCOM == 103
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;다리를 빤다(플레이어)
;-------------------------------------------------
IF SELECTCOM == 104
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;귀두 비난하는 사람 코키(플레이어)
;-------------------------------------------------
IF SELECTCOM == 106
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;귀두 고문 페라(플레이어)
;-------------------------------------------------
IF SELECTCOM == 107
	PRINTFORML 
	;이 캐릭터가 주도권을 가지고 있다
	IF IS_INITIATIVE(TARGET)
		;복종 이외
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	;이 캐릭터가 주도권을 가지고 있지 않다
	ELSE
		;연모 or 복종
		SIF ! IS_SLAVE(뱌쿠렌)
			CALL SEXVOICE_PLAYER_K77
	ENDIF
	PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 셀프 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 108
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;꼬리 셀프 Ａ삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 109
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;자위 Lv2 이상
			IF ABL:자위 >= 2
				;PRINTFORMW 
			;자위 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;땅에 엎드려 조아림(플레이어)
;-------------------------------------------------
IF SELECTCOM == 110
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;졸라댐(플레이어)
;-------------------------------------------------
IF SELECTCOM == 111
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;노출 플레이(플레이어)
;-------------------------------------------------
IF SELECTCOM == 112
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;음모 관리(플레이어)
;-------------------------------------------------
IF SELECTCOM == 113
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;신체에 낙서(플레이어)
;-------------------------------------------------
IF SELECTCOM == 114
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;소변보기 한다(플레이어)
;-------------------------------------------------
IF SELECTCOM == 130
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;오줌을 건다(플레이어)
;-------------------------------------------------
IF SELECTCOM == 131
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;입안 소변보기(플레이어)
;-------------------------------------------------
IF SELECTCOM == 132
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;이뇨제(플레이어)
;-------------------------------------------------
IF SELECTCOM == 140
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;관장(플레이어)
;-------------------------------------------------
IF SELECTCOM == 141
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;관장＆플러그(플레이어)
;-------------------------------------------------
IF SELECTCOM == 142
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;방귀 한다(플레이어)
;-------------------------------------------------
IF SELECTCOM == 150
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;안기방귀(플레이어)
;-------------------------------------------------
IF SELECTCOM == 151
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;배설 Lv2 이상
			IF ABL:배설 >= 2
				;PRINTFORMW 
			;배설 Lv2 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 소환(플레이어)
;-------------------------------------------------
IF SELECTCOM == 200
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 201
	;처음
	IF IS_COM_FIRST()
		;주인공이 처녀
		IF TALENT:MASTER:처녀
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		;주인공이 비처녀
		ELSE
			;연모 or 복종
			IF TALENT:연모 || TALENT:복종
				;PRINTFORMW 
			;그 이외
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 Ａ삽입(플레이어)
;-------------------------------------------------
IF SELECTCOM == 202
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 클리 꾸짖고(플레이어)
;-------------------------------------------------
IF SELECTCOM == 203
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수가슴 애무(플레이어)
;-------------------------------------------------
IF SELECTCOM == 204
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 오나홀(플레이어)
;-------------------------------------------------
IF SELECTCOM == 205
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 코키(플레이어)
;-------------------------------------------------
IF SELECTCOM == 206
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 착유(플레이어)
;-------------------------------------------------
IF SELECTCOM == 207
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 긴박(플레이어)
;-------------------------------------------------
IF SELECTCOM == 208
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수구욕(플레이어)
;-------------------------------------------------
IF SELECTCOM == 210
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 자위(플레이어)
;-------------------------------------------------
IF SELECTCOM == 211
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			;촉수 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			;촉수 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		;그 이외
		ELSE
			;촉수 Lv3 이상
			IF ABL:촉수 >= 3
				;PRINTFORMW 
			;촉수 Lv3 미만
			ELSE
				;PRINTFORMW 
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 산란(플레이어)
;-------------------------------------------------
IF SELECTCOM == 212
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;촉수 A산란(플레이어)
;-------------------------------------------------
IF SELECTCOM == 213
	;처음
	IF IS_COM_FIRST()
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	;2회째 이후
	ELSE
		;연모 or 복종
		IF TALENT:연모 || TALENT:복종
			;PRINTFORMW 
		;의존도 300이상
		ELSEIF CFLAG:의존도 >= 300
			;PRINTFORMW 
		;그 이외
		ELSE
			;PRINTFORMW 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

RETURN 0

;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어로도 타겟에서도 불려 간다)
;  플레이어·타겟의 구별이 없는 커맨드는 여기에 말을 기술한다
;=================================================
@KOJO_COM_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0
ENDIF
;-------------------------------------------------
;퍼스트 키스 실행
;(KOJO_EVENT(MTAR:0, 1)가 없어진 때문 이식겸 250직치는 것
;키스·버드·소프트·딥 겸용
;-------------------------------------------------
IF GROUPMATCH(SELECTCOM , 20, 340, 341, 342) && CFLAG:250
	;퍼스트 키스 플래그
	CFLAG:250 = 0
	PRINTFORML 
	;주도권은 타겟
	IF IS_INITIATIVE(TARGET)
		;연모
		IF IS_LOVER(뱌쿠렌)
			PRINTFORML 「…… 애래 봐도 처음이었던 것이에요……?」
		;복종
		ELSEIF IS_SLAVE(뱌쿠렌)
			;PRINTFORML 
		;그 이외
		ELSE
			PRINTFORML 「…… 그런 생각이 아니었던 것입니다……
			PRINTFORML   조금이라도 양심이 남아 있다면………… 네…… 잊어, 주세요……」
		ENDIF
	;주도권은 플레이어
	ELSE
		;연모
		IF IS_LOVER(뱌쿠렌)
			PRINTFORML 「…… 기쁠 것인데…… 이런 곳에서는 복잡합니다……」
		;복종
		ELSEIF IS_SLAVE(뱌쿠렌)
			;PRINTFORML 
		;그 이외
		ELSE
			PRINTFORML 「…… 아무리 그래도, 너무 슬픕니다…… 아…… 아니…… 떼어 놓아 주세요……!」
		ENDIF
	ENDIF
	PRINTFORMW 
ENDIF

;=================================================
;●커맨드 실행시(이 캐릭터가 타겟측 때)
;=================================================
@KOJO_COM_TARGET_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0
ENDIF


;=================================================
;●커맨드 실행시(이 캐릭터가 플레이어측 때)
;=================================================
@KOJO_COM_PLAYER_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0
ENDIF


;=================================================
;●커맨드 실행 후(파라미터의 변동 처리가 끝나고 나서 불려 간다)
;=================================================
@KOJO_COM_AFTER_B_K77
#DIM 뱌쿠렌
#DIM 묘우렌
뱌쿠렌 = NAME_TO_CHARA("뱌쿠렌")
묘우렌 = NAME_TO_CHARA("묘우렌")
;[공허]상태의 경우, 입이 막히고 있는 경우는 말을 표시하지 않는다
IF TALENT:공허 || IS_EQUIP_CONTINUE(TARGET , "입장착")
	RETURN 0
ENDIF

;-------------------------------------------------
;처녀 상실
;-------------------------------------------------
;주인공과의 섹스에 의한 처녀 상실
;IF TCVAR:13 == 1
;	;연모 or 복종
;	IF TALENT:연모 || TALENT:복종
;		;PRINTFORMW 
;	;그 이외
;	ELSE
;		;PRINTFORMW 
;	ENDIF
;;주인공에게 섹스 이외로 처녀를 빼앗긴다
;ELSEIF TCVAR:13 == 2
;	;연모 or 복종
;	IF TALENT:연모 || TALENT:복종
;		;PRINTFORMW 
;	;그 이외
;	ELSE
;		;PRINTFORMW 
;	ENDIF
;;그 이외의 때(시)에 처녀 상실
;ELSEIF TCVAR:13 == 3 || TCVAR:13 == 4
;	;연모 or 복종
;	IF TALENT:연모 || TALENT:복종
;		;PRINTFORMW 
;	;그 이외
;	ELSE
;		;PRINTFORMW 
;	ENDIF
;ENDIF

;-------------------------------------------------
;주인공을 사정시켰다
;-------------------------------------------------
;KOJOCOMMON에 통일

;-------------------------------------------------
;주인공을 사정시키지 않고 절정 시켰다
;-------------------------------------------------
IF NOWEX:MASTER:Ｃ절정 + NOWEX:MASTER:Ｖ절정 + NOWEX:MASTER:Ａ절정 + NOWEX:MASTER:Ｂ절정 + NOWEX:MASTER:Ｍ절정 > 0
	;연모 or 복종
	IF TALENT:연모 || TALENT:복종
		;PRINTFORMW 
	;그 이외
	ELSE
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;실금 했다
;-------------------------------------------------
SELECTCASE TCVAR:0
	;오줌 마려움이 있는 상태로 절정 강도의 합계가 4이상
	CASE 1
		PRINTFORML  ... 이미 강렬한 절정감에 심신 함께 당하고 있는%ANAME(TARGET)%가 느슨해진 비밀스런 장소로부터, 졸졸 맑은 물이 흘러넘치기 시작하고 있다
	;오줌 마려움이 높은 상태로 절정
	CASE 2
		PRINTFORML  ... 견디지 못했던 오줌 마려움이 절정의 여운으로 느슨해진 비밀스런 장소로부터, 졸졸 시미즈를 흘리고 있다
	;오줌 마려움의 한계
	CASE 3
		PRINTFORML  ... 견디지 못했던 오줌 마려움이 측에 가는 일도 할 수 없었다%ANAME(TARGET)%가 실금에 망연히 해 버리고 있다
	;공포에 의한 실금
	CASE 4
		PRINTFORML  ... 견디지 못했던 공포에 움찔움찔 체를 진동시키면서%ANAME(TARGET)%는 실금을 해 버리고 있다
ENDSELECT